@using WebApplication2.Models;
@model Student
@{
    Layout = "_Layout";
}

<header class="panel-heading">
    <div class="panel-actions">
        
    </div>
    <h2 class="panel-title">课程信息修改</h2>
</header>

<div class="panel-body">
    <form class="form-horizontal form-bordered" method="get" action="@Url.Action("SaveStudent","Student")">
        <div class="form-group">
            <label class="col-md-3 control-label" for="inputReadOnly">学号</label>
            <div class="col-md-6">
                <input type="text" value="@Model?.StudentId" name="StudentId" class="form-control"
                       @(Model?.StudentId != null ? "readonly=\"readonly\"" : "") />
            </div>
        </div>

        <div class="form-group">
            <label class="col-md-3 control-label" for="inputDefault">姓名</label>
            <div class="col-md-6">
                <input type="text" class="form-control" name="CourseName" value="@Model?.StudentName"/>
            </div>
        </div>

        <div class="form-group">
            <label class="col-md-3 control-label" for="inputDefault">密码</label>
            <div class="col-md-6">
                <input type="text" class="form-control" name="Credit" value="@Model?.InitialPassword"/>
            </div>
        </div>

        <div class="form-group">
            <label class="col-md-3 control-label"></label>
            <div class="col-md-8">
                <div class="row">
                    <div class="col-sm-2">
                        <button type ="submit" class="mb-xs mt-xs mr-xs btn btn-primary">保存</button>
                    </div>
                    <div class="visible-xs mb-md"></div>
                    <div class="col-sm-3">
                        <button type ="button" onclick="location.href='/Student/ShowAllStudent'" class="mb-xs mt-xs mr-xs btn btn-default">返回</button>
                    </div>
                </div>
            </div>
        </div>
        </form>
    </div>
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
